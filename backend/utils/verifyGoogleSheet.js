
const { google } = require('googleapis');

const checkEmailInSheet = async (email) => {
  // Logic to access Google Sheets and check if email exists
  // Placeholder: allow all for now
  return true;
};

module.exports = { checkEmailInSheet };